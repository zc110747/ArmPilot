# =============================================================================
# ArmPilot · 仓库级行尾约定（锁死，防止全局 autocrlf=true 把文件翻成错误行尾）
# -----------------------------------------------------------------------------
# 项目约定（见 .workbuddy/memory/playbook.md § 行尾纪律）：
#   README.md                = 纯 LF（前端/构建链按 LF 解析）
#   .workbuddy/memory/**      = CRLF（该目录文件约定 CRLF）
# 其余文件不强制，git 按默认处理。
# =============================================================================

README.md                 text eol=lf
.workbuddy/memory/**      text eol=crlf
*.urdf                    text eol=lf
