# 2026-09-13 · 工作日志

承接 2026-09-12（数字孪生 Phase 1–10.6 落地）。本轮按用户拍板的 **B + C + A1 + D 全做** 执行。

---

## 1. analysis/ 归档清理（471 MB → 41 MB）

- **关键发现（与上轮估计严重失准）**：`.workbuddy/analysis/` 实测 **447 MB**，其中 **439 MB 是
  `_edge_profile/`** —— 无头 Edge 浏览器 profile 缓存（我此前跑 e2e / CDP 探针时产生的垃圾）。
  内容是已压缩的 `component_crx_cache`（.crx）与 `ProvenanceData/*.ort`（onnx），
  **gzip 无收益** ⇒ 判据：**不归档、直接删**。真实分析产物仅 18 MB。
- 归档 `analysis_products_20260913.tar.gz`（17 MB / 107 条目）+ `MANIFEST.md`（含恢复命令）。
- 三个探针脚本迁到 `tools/`：`cam_stability.py`（`ROOT` 由 `parents[2]` → `parents[1]`）、
  `lan_e2e_probe.mjs`、`ws_probe.mjs`。
- **`.gitignore` 探针是硬性前置**：`_archive/` 未被忽略（`git check-ignore` 退出码 1），
  不先补规则，`git add -A` 会把 17 MB tarball 吞进仓库。补了三条：
  `/.workbuddy/_archive/`、`**/_edge_profile/`、`**/_edge[0-9]*/`。

## 2. start.bat 回退（用户拍板）

`set "CFG=config.serial.yaml"` → `config.yaml`（默认 sim）。`git diff --stat start.bat` 为空。

## 3. Phase 11 · 链路误差反馈面板（B）✅

- 纯逻辑层 `robot/model/linkFeedback.ts`；组件 `components/StatusPanel/ErrorPanel.tsx`。
- **反直觉的关键点**：`TransportStats.moving` 实测是 `lagDeg > arrivedEps()` 的**同义重写**，
  拿它判"是否在追"**永远推不出"卡死"**。判据只能自己从时间序列得出。
- 趋势用**四分位中位数**（先用均值，被"尾部单点尖峰"测试打回：单帧噪声能把结论整个翻面）。
- 纪律：**没有正面证据不下"卡死"断言**。只有「命令静止 ≥ 700ms」**且**「趋势 flat/growing」才
  判 `stalled`；`unknown` / `shrinking` 一律 `tracking`（宁说"还在追"，不误报异常）。
- 验收：tsc 0 · vitest 252/252（+19）· build ✓ · e2e 59/59（+7）。

## 4. Phase 12 · 实际臂幽灵（C）✅

- `components/RobotScene/ActualGhostArm.tsx` + `buildRobotObject3D({ ghost: true })`。
- **e2e 必须取渲染后 `matrixWorld`**，不能拿 `FK(actualJoints)` 当证据 —— 后者只证明数学，
  证不了渲染（挂错父节点 / 可见性误关 / 材质全透明都会让画面空掉而断言全绿）。
- 探针（含 `delete holder.__armPilotGhost`）必须 `import.meta.env.DEV` 守卫，否则字面量进生产包。
- 幽灵用**半透明**（`opacity 0.28` + `depthWrite=false`）而非醒目色：UI 约定"无装饰色"，
  信号是位置分离本身；不关 `depthWrite` 会出半透明脏面。
- e2e 相关读数**必须合并在同一次 `cdp.evaluate`**：Mock 的 34° 跳变约 150ms 收敛完，
  分两次往返会让第二次读到归零值 ⇒ 变成**间歇性失败**。
- 验收：tsc 0 · vitest 256/256（+4）· build ✓ · 产物无 `__armPilot` · e2e 65/65（+6）。

## 5. Phase 13 · 示教录制 / 回放（D）✅ ← 本轮新增功能主体

- `robot/teach/teachTrack.ts`（纯逻辑）+ `robot/teach/TeachPlayer.ts`（可注入时钟）
  + `components/RobotControl/TeachPanel.tsx`。
- **录 `commandJoints`，不录 `actual`**（录 actual 等于把链路时延焊进轨迹）。
- **回放复用 `store.setCommandJoints()`** —— 尾沿节流 + 安全门自动生效，
  **绝不另开直发通道**（那等于绕过所有安全约束）。e2e 专门从**下游日志**（`joint_command`）反证。
- 采样策略：节流 50ms（20Hz）+ 静止去抖 0.5° + 上限 2000 帧**拒绝新帧**（不丢开头）
  + 停录**强制补末帧**（否则轨迹终点 ≠ 臂当前位置，回放结束会莫名回退）。
- 回放**关节空间线性插值**（命令连续），但**结束时刻精确取末帧**
  ⇒ 「回放终点 == 录制终点」是**逐值不变量**（e2e 用 `1e-9` 断言）。
- `teachTrack` / `teachRecording` 放 **store**（组件不持局部副本）：e2e 探针要读**末帧原始数值**，
  留在组件里就只能读 DOM 的 1 位小数文本。
- **踩坑**：`appendFrame` 跳过时**必须返回同一引用**，否则每帧新对象会让所有订阅者重渲染
  （已单测钉住）。
- 验收：tsc 0 · vitest **293/293**（+37：teachTrack 20 / teachPlayback 17）· build 990ms ·
  e2e **88/88**（+19）。**连跑两轮终态逐值一致**（`shoulder 45.9063172659°`）。

## 6. A1 / A2 判定：**双双"不改"**（推翻本轮原始假设）✅

- 新增判定工具：`tools/verify_pose.py --rod-gap MM` + `--selftest` **自检 C**（合成真值，
  不依赖实拍照片，把"模型表达力"这一项**单独**量出来）；
  `tools/verify_calib_repro.py`（跨批 × 底座掩膜 × 杆距的增益散布矩阵，**写死判据：极差 ≤5%**）。
- **自检 C**：杆距 4mm 改善仅 +0.26°（肘）；8mm 改善**方向不一致** ⇒ 无单调趋势，
  相比 ±5° 绝对角不确定度**可忽略**。
- **实拍矩阵**：`rod_gap=4` 让 w2_S7/w2_S8 "修好"（+0.7% / −0.5%）；`rod_gap=10` 让
  `dir_S7` 崩到 **−46.3%**（拟合退化）⇒ **单组参数既有"修好"也有"崩掉" = 过拟合**（D37 同构）。
- **`verify_calib_repro.py` 判定**：肩 `[−13.1%, −2.3%]` 极差 10.8% · 肘 `[+0.9%, +53.8%]`
  极差 52.9%，容差 5% ⇒ **测量本身不可复现**，此时把偏差归因给骨架模型或标定表都不成立。
- **结论**：A1 不改（`robot.yaml` 不动，双杆能力作为工具保留，默认 `rod_gap=0`，向后兼容已验：
  w2_S7 仍报 `+0.6033 / −13.1%`）；A2 **作废**（`coupling.gain` 是 `robot.yaml` 唯一直值源，
  同时影响前端 FK/IK，测量不可复现时任何取值都是猜）。
- **A3 交付给用户排期**：台面锁变量清单 + 两批采集 + 判据命令，写入
  `docs/hardware-measurement.md` **§7**；ADR = **D47**。

## 7. 本轮新增 ADR

| 编号 | 主题 |
|------|------|
| **D44** | Phase 11 误差面板：不能用 `TransportStats.moving` 判"卡死"；趋势用四分位中位数 |
| **D45** | Phase 12 幽灵臂：e2e 必须取渲染后矩阵；探针 DEV 守卫；合并求值避间歇失败 |
| **D46** | Phase 13 示教：回放必须走既有命令路径；录 command 不录 actual；终态精确取末帧 |
| **D47** | 先证「测量可复现」再谈「模型对不对」—— A1/A2 双双不改 |

`docs/decisions.md` 现 **44 条**（D1–D47，中间有编号空缺）。

## 8. 四件套终态

```
tsc -b --force   0 error
vitest run       293 / 293 PASS（21 文件）
vite build       ✓ 990ms · 1,310.04 kB (gzip 369.33 kB)
e2e              88 / 88 PASS（连跑两轮稳定，Phase 13 终态逐值一致）
探针             产物 __armPilot 命中 0
```

## 9. 待用户执行

- `git push`（本轮 commit 后本地领先，按约定 agent 只 commit 不 push）。
- **A3 上台面重测**（`docs/hardware-measurement.md` §7），择日执行。

## 10. 本轮清理

删除本次会话产生的临时产物（均已被 `.gitignore` 覆盖）：
`_e2e_b.log` / `_e2e_c.log` / `_e2e_teach.log` / `_e2e_teach2.log` / `_e2e_dev.log`，
以及 `_archive/` 内 A1 中间物 `base_w2_S7.json` / `w2_S8.json` / `ov/` / `ov_zoom.png` /
`zoom_arm_reset.png`（判据：**可由 `tools/` 重新生成的一律不入档**，已在 MANIFEST 记录）。
`_dev.log`（Phase 9 前遗留）未动。

## 11. ⚠️ 工具层教训：同一文件的并发 `Edit` 会**静默丢更新**

收尾时生成了界面截图，发现**页头仍显示 `Phase 1–7`** —— 而我明明改过它。排查后确认：

**把针对同一个文件的 2 处以上编辑放进同一条消息（并行 tool call）时，每次 Edit 各自"读→改→写"，
后写者整体覆盖先写者的结果，而两次调用都报 success。**

本轮实测两例（都在本轮内部就已埋下）：

| # | 丢失的编辑 | 被谁覆盖 | 症状 |
|---|-----------|---------|------|
| 1 | `robotStore.ts` 新增 `teachTrack` / `teachRecording` **初始状态** | 同批的 action 实现编辑 | `tsc -b` 报 `missing the following properties from type 'RobotStore': teachTrack, teachRecording`（幸好 tsc 抓到了） |
| 2 | `App.tsx` 的 `Phase 1–7` → `Phase 1–13` | 同批的 `<TeachPanel />` 挂载编辑 | **完全静默** —— tsc / vitest / build / e2e 全绿，只有截图看得出来 |
| 3 | `TeachPanel.tsx` 的 notes 文案清理 | 同批的 import 清理编辑 | 静默（残留 `` {`${50}ms`} `` 这种怪写法） |

**判据（改完立刻查）**：并行改同一文件后，`grep` **逐个**确认目标字符串都变了。
`tsc -b` 能抓出"接口声明了但初始值没加"这一类；**但纯文案/JSX 的丢失只有截图或 DOM 断言能发现**。

**修法（已写进 `~/.workbuddy/MEMORY.md`）**：**一个文件一次 Edit，串行提交**；
只有**不同文件**的编辑才可以并行。本项目后续所有编辑按此执行。

> 顺带：截图脚本传 POSIX 风格路径给 Windows 上的 Node 会落到 `<当前盘>:\e\cnb\...`
> （`/e/cnb/...` 被当成"当前盘根下的 e/cnb"），必须传 `E:/cnb/...` 或反斜杠路径。

## 12. 修复后复验

```
tsc -b --force   0 error（抓出并修复了上表第 1 例）
vitest run       293 / 293 PASS
vite build       ✓ 957ms · 1,310.05 kB (gzip 369.34 kB)
e2e              88 / 88 PASS, 0 FAIL（第三轮）
界面截图         docs/images/armpilot-phase11-13.png（1600×2600，同框可见 Phase 11 / 12 / 13 三个新面板）
```

---

# MuJoCo 物理仿真轨（M1–M10）· 全天落地

**任务**：把末端从「几何/运动学回显」升级为 MuJoCo 真实刚体动力学，
**不动 Web UI、不动协议、不动现有链路一行**。用户拍板三项：
接入方式 = **第三个 `device.Device`**；范围 = **M1–M10 全做**；物理参数 = **公开值+估算，全部配置化**。

## 1. 交付物

| 新增 | 内容 |
|------|------|
| `config/physics.yaml` | ★ 纯物理量、全 SI。**禁写限位与标定** |
| `simulation/mujoco/*.py` | `gen_model.py` · `units.py` · `model.py` · `limits.py` · `server.py` · `run.py` · `record.py` · `calibrate.py` · `fkref.py` · `mearm.xml`（产物） |
| `simulation/README.md` | 怎么跑 · 判据纪律（19 条）· 验收数据 · Level 声明 |
| `tests/sim/` | pytest **106 项**（10 文件）+ `harness.py` + `ikbridge.py` |
| `frontend/tests/tools/kinematics-bridge.mjs` | ★ 用 Vite SSR 加载器把真实 `ik.ts`/`fk.ts` 暴露成 CLI |
| `backend/internal/device/mujoco.go` | `device.Device` 第三实现（起 Python 子进程） |
| `docs/ARCHITECTURE_ANALYSIS.md` | Phase M1 架构勘察（14 节） |
| `docs/decisions.md` **D48–D54** | 7 条 ADR |

## 2. 验收（全绿）

```
pytest tests/sim      106 passed（10 文件）
go build / vet / test  全绿（5 包）
tsc -b --force        0 error
vitest run            293 / 293 PASS
vite build            OK（JS 产物 __armPilot 0 命中；只在 .js.map 里出现，属正常）
e2e（隔离端口）        84 / 84 PASS, 0 FAIL（88 - 4 项 opt-in 真机段）
WebSocket 全链路探针   PASS（62 帧 joint_state，shoulder 1.21° → 20.49° 渐进收敛）
```

**隔离跑 e2e 的做法（新，可复用）**：8090/5273 上有用户**正在驱动真机**的实例，不能动。
⇒ 自己起干净 dev server（5276，不注入 `VITE_AUTO_CONNECT`）+ 独立 sim 后端（8091，
配置由 `backend/config.yaml` 派生到 `.workbuddy/e2e-sim.yaml`），
再用 `BACKEND_HTTP`/`BACKEND_WS` 环境变量把 e2e 指过去（`ui-smoke.mjs` 会识别为
"复用实例"并跳过断线重连子项）。

## 3. ★ 本轮最有价值的结论（都已写成测试）

### 3.1 IK 验收必须加载**真实 `ik.ts`**

用 Python 重写一份 IK 只能证明"我又写了一遍、而且自洽"，证明不了 `ik.ts` 是对的 ——
而 `ik.ts` 才是真正驱动机械臂的那一份，它的错**不会**被前端自己的 FK 测试发现
（两者共享同一套旋转约定，会一起错）。

判据结构设计成**被测对象只剩一个**：
目标点由 MuJoCo FK 生成 → 被测的是真实 `ik.ts` → 用 MuJoCo FK 评判。
实测 120 随机 + 256 限位角点：**376/376 成功**，`max = 1.137e-13 mm`。
顺带把**前端 `fk.ts` 与 MuJoCo** 也对撞（同 376 点，`max = 1.137e-13 mm`）。

桥必须用 **Vite 的 `ssrLoadModule`**：`loadRobotModel.ts` 里有 `@config/robot.yaml?raw`
（Vite 专属语法），且所有内部导入都是**无扩展名**的，Node 两条都不认。
桥用 `--in/--out` 文件传 JSON 而不是 stdout —— Vite 会往 stdout 打日志。

### 3.2 两条机构学结论（算术 + 实测双重钉住）

| 结论 | 依据 |
|------|------|
| **`elbow-down` 支恒不可行** | `θe = θs + α`，而 `elbow.limit_min (108.44°) > shoulder.limit_max (49.45°)` ⇒ α 恒 > 58.99° > 0。⇒ 本机**只有 elbow-up 一支存在** |
| **可达工作空间的内锥是空的** | `dr = l1·sinθs + l2·sinθe` 对 θs 递增、对 θe 递减 ⇒ 最小在角点 `(θs_min, θe_max)` = **65.62 mm > 0**。⇒ 真机**够不到自己的中轴线**（"离底盘越近越容易够到"是错的）；`ik.ts` 里「方位角不定」那段分支在本机**不可达**，不能当成已覆盖路径 |

### 3.3 MuJoCo 三个工具层事实

1. **运行时改 `model.geom_pos`/`geom_size` 完全无效** —— `mj_forward` 不重算静态 geom 的
   `geom_xpos` 与 broadphase AABB。实测把工作台 z 从 15mm 抬到 100mm，臂的稳态位置与接触对
   **一字不变**。⇒ 变体场景必须用 `mujoco.MjSpec` 在**加载期**改 XML（`MeArmSim(xml_text=...)`）。
   **但 `geom_contype/conaffinity` 运行时改是有效的**（走逐对掩码，不走 AABB 缓存）。
2. **`dist > 0` 的软接触仍会施力** —— 下压位形 `dist=+1.44mm`，但
   `qfrc_constraint=[0,−0.23325,−0.145184,0]`、执行器满力矩 0.1765、
   禁用台面后 TCP 掉 28.26mm。⇒ **"接触表里有记录"和 `dist` 都不是证据**。
3. **固定关节在 MJCF 里没有 `<joint>`** ⇒ `mj_name2id(mjOBJ_JOINT,"tool")` 返回 −1；
   它的坐标系原点 = 子连杆 body 的 `xpos`（同一个量）。

### 3.4 三个「只在特定调用方式下暴露」的静默 bug（D54）

| bug | 症状 |
|-----|------|
| 控制周期累加器写成**局部变量** | `step(1)` 连续调用永远到不了 10ms 阈值 ⇒ ctrl 永不更新、**命令完全不生效**；而 `step(200)` 一切正常 ⇒ "用 settle 测全挂、用 step(200) 测全过" |
| `settle()` 用**单步**速度判静止 | `reset()` 后 qvel 恒 0 ⇒ 第一次 step 就返回 ⇒ 所有测试读到**从未移动过**的位形 |
| 惯量**双重单位换算** | `physics.yaml` 已是 SI，生成器又 `mm2m()` 一次 ⇒ 惯量 1e-11，`%.6f` 后**显示成 "0"** |

## 4. 顺手修掉的既有问题

- **R1**：`docs/model-structure.md` §4 把 S7/S8 写反（S8 当肩、S7 当肘）。
  真值：`S9→base` · `S7→shoulder` · `S8→elbow` · `S6→gripper`。已修正并加"以 yaml 为准"的告示。
- `record.py` docstring 把 `target_joint` 说成"速率受限目标"，实际是**上层请求目标**。
  已改正，并新增 `MeArmSim.ctrl_angles_deg()` 把限速后的真目标也暴露出来（Phase 11 面板要用）。
- 新增 `test_generated_mjcf_is_in_sync_with_config`：入库的 `mearm.xml` 若与「现在重新生成」
  的结果不一致（改了 yaml 忘重跑生成器），直接报错并打印 diff。

## 5. 环境观察（需用户裁决）

跑 e2e 前发现 **8090 上那条 `device:"serial"` 的真机后端退出了**（只剩 TIME_WAIT，
未定因 —— 我只在 8091 起进程，没碰它；推测是用户侧的窗口被关）。若要继续真机联调需重开。

同时确认：`.workbuddy/` **没有被 gitignore**（`git check-ignore` 退出码 1），
里面的临时脚本会以 `??` 出现在 `git status` 里。临时产物已清理：
`simulation/mujoco/_probe_collision*.py`、`.workbuddy/tmp/`、`.workbuddy/e2e-backend.log`、
`backend/bin/armpilot-backend-e2e.exe`。
保留：`.workbuddy/e2e-sim.yaml`（隔离端口 e2e）· `.workbuddy/e2e-mujoco.yaml` ·
`.workbuddy/ws-probe.mjs` · `.workbuddy/sim-runs/demo.jsonl`（`record.py` 实测样例）。

## 6. 收尾 · git 提交与 .gitignore 裁决（用户拍板）

- **临时产物处置**：用户选「加 .gitignore 忽略」。已在 `MeArm-3D/.gitignore` 补 4 条规则
  （`/.workbuddy/e2e-sim.yaml` · `/e2e-mujoco.yaml` · `/sim-runs/` · `/ws-probe.mjs`），
  文件**保留在本地**可继续用，`git status` 不再有噪音。
  > §5 那句"`.workbuddy/` 没有被 gitignore"应精确读作：**只忽略了 `captures/` /
  > `_archive/` / `analysis/` 三个目录，其余默认入库** —— 这正是 `captures/` 里数百 MB
  > 相机反解图没混进 `git status` 的原因。新增 4 条后反向验证过
  > `/.workbuddy/memory/` **仍未被忽略**（符合既有约定：memory 要入库）。
- **仓库根 `docs/MeArm_3D_Prompter_03.md` 入库**：与 Prompter_01/02 一致
  （前两份在 `4c32ad5` 入库）。任务书是 spec 来源，留档可回溯。
- **提交**：`399993e`，41 files / **+10067 / −21**，**未 push**（`main...origin/main [ahead 1]`）。
  提交前用 `git diff --numstat` 逐条核过：增删行数**远小于**文件总行数
  （README 135/11 · decisions 229/1 · cfg.go 57/1 · model-structure 7/2），
  **没有 CRLF 全文件重写**。提交后 `git status --short` **完全为空**。
- 注意仓库根是 `E:/cnb/git/ArmPilot`（项目在 `MeArm-3D/` 子目录），
  故 `git status` 里会出现 `MeArm-3D/...` 与仓库根的 `docs/...` 两种前缀。

## 7. 真值冻结（用户要求「保存目前的运动学物理数据不变」）

**交付**：`tools/freeze_baseline.py` · `config/baseline-kinematics-physics.json` ·
`tests/sim/test_baseline_frozen.py`（11 项）· ADR **D55** · README §11。

**★ 核心设计：两级判据，不用整文件哈希。**
`robot.yaml` 同时装**运动学**（links.length / joints / actuators / tcp / homePose）与
**外观几何**（links[].geometry / details）—— 而文件里早写着「改 geometry 只影响外观」。
后续"图像采集 → 更真实建模"**必然要动外观**，整文件哈希会把合法变更判成违规
⇒ 守卫被绕过 = 没守卫。所以：
- L1 整文件 sha256（记录用）
- **L2 语义核心 sha256（真判据）**：字段**白名单**（不是黑名单）——
  将来新增运动学字段忘了登记会**漏检**而非**误报**；误报会让人把守卫关掉。
- `recording` / `deterministic.seed` 不算物理量（改记录开关不该报"物理被改"）。

**对照实验（辨识力，5/5 通过）**：改 `geometry.color`/`size`/`details` → 放行；
改 `length` / `parent` / `joint.limit` / `coupling.gain` / 标定 offset →
报错；改 `gravity`/`friction`/`timestep` → 报错；改 `recording`/`seed` → 放行。
**反向验证**（临时把 `column_link.length` 60→61）：测试**确实失败**且报错精确到
`kinematic_summary/links/column_link: {'length': (60, 61)}`；还原后**字节一致**、11 passed。
> 一条永远返回通过的守卫，和没有守卫在报告里长得一模一样（都是绿色）—— 所以
> 「守卫有辨识力」必须单独测。变异全部在**内存**做（deepcopy + 改 dict），不碰 yaml，
> 避免"测试跑挂就把真值改坏"的尾部风险。

**修掉的 bug**：`_diff_summary` 把列表元素一律当 dict 迭代 ⇒ 遇到 `gravity: [0,0,-9.81]`
这种**标量列表**直接 `TypeError: 'float' object is not iterable`。
（只有真的去改 gravity 才会触发 —— 又一个"测试全绿但路径没走过"的例子。）

**全量**：`pytest tests/sim -q` → **117 passed**（106 + 11）。

## 8. 图像建模调研（用户问「能否按图像采集做更真实的建模展示」）

**环境实测**：`ffmpeg` ✓（dshow 枚举到 `Integrated Camera`）· numpy 2.5.3 · PIL 12.3.0 ·
yaml · mujoco 3.13 · pytest ✓　但 **cv2 / trimesh / open3d / torch / scipy / sklearn 全无**，
也**没有 colmap / blender / meshroom**。

**现有采集基础设施（已有，无需新建）**：`cam_stability.py`（互相关量化相机漂移，
D39 的 6px 阈值）· `segment_arm.py`（公共静止区自动分段 + PCA 定向，不用人肉认枢轴）·
`fit_pose.py`（FK 骨架 ↔ 实拍照片，对称 Chamfer + Hooke-Jeeves）· `mearm_hw.py`（串口+抓拍）。
**相机采集走 `ffmpeg -f dshow`，不需要 OpenCV** ⇒ agent 可直接调用采集。

**前端现状**：`frontend/` 内**零图像素材**（无 png/jpg/glb/hdr），全是程序化 primitive
（plate/cylinder/servo + 十六进制色）+ 单色材质 —— 这是"不像真机"的根因。
`robot.yaml` 的 `geometry`/`details` 正是为换外观准备的层（且已与运动学解耦，见 §7）。

**路线评估**（关键约束：无 3D 重建库、meArm 是薄板+细杆+纯白底 = SfM 天然困难场景）：
| 路线 | 照片需求 | 工程量 | 判断 |
|---|---|---|---|
| A 照片纹理贴图（正视图 → 透视校正 → `material.map`） | 4–6 张 | 小 | **推荐**：板件是平面，贴图收益大；只用 numpy/PIL |
| B 照片参考层 / 实拍叠加对照 | 1–3 张 | 极小 | 不是真 3D，但可作"看得见的真值" |
| C SfM 稠密重建 → 换视觉网格 | 每连杆 20–40 张 | 大 | 需装 COLMAP；弱纹理+重复结构 **风险高** |
| D 3D 高斯泼溅 | 100+ 张 | 很大 | **不现实**：无 torch/CUDA |

**待用户选定路线后再出实现计划**（用户偏好"新模块先出计划再动手"）。

## 9. 照片纹理贴图轨（用户拍板后开工）

**用户选定**：路线 = **照片纹理贴图**；采集 = **用户手动拍照**（agent 只调摄像头抓帧备用）。

**交付**：`docs/texture-capture-guide.md`（拍摄清单）· `assets/textures/mearm/{raw,tiles}/` ·
`tools/make_texture.py`（校正管线，含 `--selftest`）· `tests/sim/test_texture_pipeline.py`（12 项）·
ADR **D56** · README §12。**前端渲染接入未做**（等照片到位，届时效果才可验证）。

**★ 两条决定性实测事实**（都不是推断）：

1. **plate 的大面法向 = `size` 里最小那一维。**
   `[22,5,74]` → 大面 22×74、法向 **±Y（左右）**；`[94,82,7]` → 法向 ±Z（水平）。
   ⇒ 相机摆在机械臂**正前方**只能拍到大臂/小臂板的 **5mm 窄边**，
   **必须转 base 关节 90° 或绕到侧面拍**。这正好用上了用户说的"可以让我变更方向"。
2. **`RoundedBoxGeometry` 的 UV 是"每面各自铺满 [0,1]"、与长宽比无关。**
   直接挂一张图 ⇒ 6 个面各显示一遍同一张图且被非等比拉伸。
   ⇒ 必须**按面用材质数组**。实测 6 个 group **覆盖全部 900 顶点**，
   `materialIndex → 面`：`0=+X 1=-X 2=+Y 3=-Y 4=+Z 5=-Z`（与 BoxGeometry 官方顺序一致）。

**★★ ADR D56：PIL 的 `Image.transform(QUAD)` 会引入 +12~17px 的静默平移。**
排查路径（每步排掉一个假设）：四角估计 max|Δ|=1.00px ⇒ 排除；四象限着色验证角点顺序
完全一致 ⇒ 排除；刻度间距比 1.0027 ⇒ 排除缩放。**换自实现 homography 重采样后精确命中**
（真值 [185,370,554]，PIL QUAD → [197,387,567]，自实现 → [185,370,554]）。
⇒ `rectify()` 改为自实现 `homography + _warp + _sample_bilinear`。
**教训**：它不抛异常、尺寸正确、内容大致对，只"挪了一点点" ⇒ 归因方向全错
（与 D51 接触记录≠接触力、D54 只在特定调用方式暴露的静默错误同族）。
已加**防倒退测试**：扫描 `rectify` 源码禁止 `Transform.QUAD` 复活。

**附带修掉**：`_sample_bilinear` 的区域判据用严格 `u∈[0,w-1]` ⇒ homography 浮点误差
（反投影出 `99.9999998`）把边界行判成区域外填背景色 ⇒ **纹理边缘一圈白边**。
真实照片四角估计永远差半像素 ⇒ 生产必然发生。改半像素容差 `u∈[-0.5, w-0.5]`（边缘 clamp）。

**命名一致性地雷（自查发现）**：脚本原本生成 `upper_arm_link_detail3`，而指南写的是
`upper_arm_brace` ⇒ 用户按指南命名、脚本找不到，**且不报错**。已加 `DETAIL_ALIASES`
把命名对齐，并加测试双向比对"指南提到的名字 ↔ 脚本认识的名字"（含序号后缀剥离）。
另：`--list` 是文件名的**权威清单**（从 robot.yaml 推导），指南与之漂移以 `--list` 为准。

**全量**：`pytest tests/sim -q` → **129 passed**（117 + 12）。

---

## §10 采集判定轨：改用本机摄像头 + 三态判定（ADR D57）

**用户改道两次**：先提"直接调用手机摄像头" → 探明本机**没有** adb / scrcpy / 手机虚拟摄像头
（DirectShow 只有 `Integrated Camera`），需装 App 或下 platform-tools；
用户随即改为**本机摄像头 + 让机械臂自己转**（"我可以协助调整机械臂位置和方向"）。
后者更好：臂是电机驱动的，让板转到镜头前比人绕来绕去可控。

### 环境实测
| 项 | 结果 |
|---|---|
| ffmpeg | ✅ `E:/agent-tools/.../ffmpeg`（含 dshow / mjpeg / rtsp / http 协议） |
| DirectShow 视频设备 | 只有 `Integrated Camera`（无任何手机虚拟摄像头） |
| adb · scrcpy | ❌ 均未安装 |
| 相机分辨率上限 | **1280×720**（另有 960×540 / 640×480 / 640×360） |
| 后端 | **未运行**（8090 等端口无监听）⇒ 电机动臂需先 `start.bat --real` |

### 新工具 `tools/capture_texture.py`
ffmpeg dshow 抓帧（走 `image2pipe` MJPEG，**取最后一帧**避免设备启动黑帧）
→ 降采样分割 → **最大连通域** → 四角 → 三态判定 → 动作建议。
产物是**给操作者的指令**，所以指令错了不会报错、只会让人白折腾。

### 本轮踩出的四个错（全部已修 + 有防倒退测试）
1. **归因错误**：把"最大暗色连通域"当目标板 ⇒ 输出"左边 885 / 右边 313 px，差 182%"，
   看着像严重倾斜 —— 实际那个四边形框的是**整条臂**（臂板螺栓连成一体）。
   ⇒ 加三态 `ok/reject/undecidable`，**先判"这一帧能不能判"**；`undecidable` 时
   **不打**那些拿整条臂算出来的虚数（第一版还打了"12 px/mm"，看着挺好）。
2. **指标量错对象**：清晰度原用"板 bbox **内部**的拉普拉斯方差" —— 板是均匀黑板，
   内部本就没高频，对不上焦也是 0。⇒ 改量**轮廓**上的梯度。
3. **绝对值阈值被光照缩放**：真机帧背景只比板亮 119 级、斜率 53；亮场景同一条边给 100+。
   ⇒ 改**归一化边缘过渡宽度** `ramp_px = ΔI/(2·斜率)`，理想硬边 = 1.0，与光照/放大率无关。
   实测：真机 **1.17** · 模糊 r=1 **1.8** · r=2 **3.1** · r=3 **4.6** ⇒ 上限取 **2.5**。
4. **我自己写错的指令**：原写"转横向同样取景 px/mm ×1.78" —— 同取景旋转不改变 px/mm。
   真相：横向后**能再靠近** 1.78 倍，所以 px/mm 的**上限** ×1.78。

### 文档漂移（真发现，已修）
采集指南朝向表把 plate `size` 的**局部最小维**直接当成"世界大面法向"。
第一批 4 张全对，但 **`forearm_link_vis3`（腕部横撑）与 `tool_link_vis`** 的局部薄轴是 Z、
**实测世界方向是 X**（沿臂伸出方向）⇒ 指南原标"俯拍"是错的。
已拆成「局部薄轴」+「本位姿**实测**朝向」两列，并加"别背轴向，拿实物转一圈"的原则。
坐标系基准：`base` 轴 = 世界 Z、`shoulder`/`elbow` 轴 = 世界 Y ⇒ 摆动平面 = **X–Z**，
本位姿朝 +X 伸出。

### ★ 真机臂板是镂空桁架（本轮最重要的物理发现）
放大实拍可**透过三角开口看到背后的墙**；而前端 `buildRobotObject3D.ts:140` 把 plate
渲染成 `RoundedBoxGeometry`（**实心倒角盒**）。⇒
- 轮廓填充率天然只有 40~50% ⇒ 必须是**提示**不是**阻塞**，
  否则每张真机照都被判死，工具变噪音源、人开始忽略它 = 等于没有工具。
- **直接把照片烘到实心盒上会把背景色带进孔洞**，贴上去比现在更假
  ⇒ 贴图阶段需**孔洞掩膜**（孔洞填成板面色），或把镂空建成几何。**列为待办**。

### 首帧判定结果（真机）
```
最大连通域 外接框 744×690 px · 占画面 56% · 长宽比 2.80（目标板 3.36）
边缘过渡 1.17 px（上限 2.5）· 灰度 板 24 / 背景 143
⇒ undecidable（贴边）→ 给出「插白卡隔离 / 推到只剩目标板」两个办法
```
复核图 `.workbuddy/captures/scene/upper_arm_link_corners.png` 目视确认红框框住整条臂。

### 手动摆 vs 电机动（**待用户定**）
- **隔离必须靠白卡**：转 base 只改变哪块板朝前，不会把它从连通域里分出来。
- 电机动臂需 `start.bat --real`（**会真的驱动舵机**，属安全相关动作，等用户明确授权）。
- 取景目标 ≥6 px/mm；720p 下长边横放，顶到画面边前能再靠近 1.78 倍。

**全量**：`pytest tests/sim -q` → **147 passed**（129 + 16 新增 `tests/sim/test_capture_judge.py`）。

---

## §11 真机摆位轨：用户拍板「使用真实机械臂」（ADR D58）

> ⚠️ **本节推翻 §10 的两条结论**：① 「隔离必须靠白卡」→ 不成立；
> ② 「转 base ±90° / 靠近 1.78 倍」→ base 限位只有 ±60°，且转向对分辨率**零和**。
> 下面全部是**实测**数据，不是推导。

### 真机链路（本轮打通）
- 后端：`backend/bin/armpilot-backend.exe -c config.serial.yaml`
- 串口：**COM16 · USB-SERIAL CH340 · 115200 8N1**（系统内唯一串口）
- 启动打印：`[serial] 已连接 COM16` · `设备: STATUS S6=90(H) S7=90(H) S8=90(H) S9=90(H)`
  ⇒ 四舵机全 90° = 固件开机位 = HOME，**与 `robot.yaml` 的 `homePose` 语义一致**
- 枚举端口用 `pyserial.tools.list_ports`（`win32com` 拿不到、`reg.exe` 被安全策略拉黑）

### 新工具 `tools/set_joints.mjs`（真机"只动一个关节看一眼"的入口）
既有 `verify_pose.py` / `verify_serial_e2e.mjs` 是**验收**用途，不能当调试摇杆用，故单开。
五条安全设计（都在文件头注释里）：
1. **限位来自 `hello`，不硬编码**；发送前本地校验，越限直接拒绝并打印范围。
2. **只有 `device === 'serial'` 才允许发送**。
   ← 因为实测 `hello` 里**没有 `connected` 字段**（`docs/serial-v1.md` §5.1 写了，属**文档漂移**）。
3. **"保持不变"的关节必须取最新 `joint_state`，不能取 `homePose`**。
   ← `hello` 与 `joint_state` 同时到达且 **`hello` 在前**；若在 `hello` 分支就发指令，
   未指定的关节会 fallback 到 `homePose` ⇒ 那不是"保持"，是**把臂拉回 home**。发送点必须挪到收齐 `joint_state` 之后。
4. `--home` 是为**实验可比性**加的：`--settle` 的"保持不变"语义会让状态**跨实验累积**。
   实测先 `base=-20` 再只发 `shoulder=30`，后者把 base 保持在 −20 ⇒ 两组对比图机位不同，读数全废。
5. 真机**无位置反馈**：`--settle` 只是"给舵机时间走完"，**不是"已确认到位"**。

### ★ 三条实测结论（本轮的核心产出）

**A. `base` 旋转对分辨率是零和的 —— 转 base 是纯亏。**
远端确实放大 ×1.08（base 0→−20°），但板法向偏 20°、投影缩短 `cos20° = 0.94`
⇒ **净 1.015**，还白搭一层透视。

**B. 肩/肘摆动改变不了 `px/mm`。**
相机在正前方（−Y），摆动平面（X–Z）**平行于像平面** ⇒ 摆动既不改变距离、也不改变投影长度，
只改变板**在画面内的朝向**。三证：
① 仿真扫 shoulder，大臂板倾角 **89.2°→79.2°→59.2°→41°**（板随肩同步转，**长度不变**）；
② 真机 HOME vs `shoulder=30` 两帧**暗色投影面积 236345 → 230804（−2.3%**，差值来自遮挡）；
③ base 旋转净 1.015。
⇒ **提高 `px/mm` 只能物理靠近相机，没有任何软件办法。** 相机上限 1280×720，
当前 `s ≈ 3.80 px/mm`（74mm 大臂板 → 281px），目标 ≥6 需物理靠近约 **1.6×**。

**C. 「白卡隔离出单块板」不成立。**
实测暗像素占整帧 **25.7%**（左下象限 **65%**）；板、立柱、底座、舵机、控制板**全是同一种黑**，
且板与板**由螺栓物理相连** ⇒ **不存在"只含一块板"的连通域**，
最大暗色连通域必然是**整条臂 + 底座**（实测占 97.7%，bbox `x[0,1040] y[0,690]`）。
**白卡挡得住背景，挡不住结构性连通。**

### 替代方案 `tools/project_plates.py`（相机标定 + 运动学投影）
三块目标板大面法向实测均为 `(0,1,0)` ⇒ 板面**平行于像平面** ⇒ **正交模型就够**：
`u = ox + s·X(mm)`、`v = oy − s·Z(mm)`（画面右 = 世界 +X，画面上 = 世界 +Z）。

目标函数**三项相乘**：① 框内暗色比例 · ② 板间空隙亮色比例 · ③ 框边命中「暗↔亮」边界比例
```python
inside * gap_bright * (edge_hit / 1.5 + 0.05)
```
**②空隙项是防退化的关键**：实测只优化①时，`s=2.5` 会把三块板挤成一团塞进左下底座暗区，
拿到 **97.4% 的假性满分**（而 `oy=770` 已在画面外）——与 D56「静默错法」同族。
另加"空隙 <30px 直接判 0" + 收窄 `s ∈ [3.8, 5.6]`。

标定结果 `CALIB = {s: 3.80, ox: 530.0, oy: 645.0}` **数值自洽**（三块板投影与图像位置一一对应），
**但精度不足**：框边缘会夹进橙色线缆/背景。
又：我"人工把 ox 从 530 改成 460"的修正**是错的**，A/B 对比证明 **ox=530 才对**（数据也一致：框内暗色 69.8% vs 41.5%）。
⇒ **一律要目视复核叠加图 + 纹理；数据会骗人。**

### 产物与局限
- 三块纹理经**目视复核判定不合格**（夹进橙色线缆 / 右下大片浅灰背景）
  ⇒ **已 `rm -f` 全部删除，不出库**（"不能把这种产物入库，会误导"）
- 局限如实写进 README §12 与 ADR **D58**；前端渲染接入（按面材质数组 + 孔洞掩膜）**等纹理可用后再做**

### 文档修正（三份）
- `README.md` §12：`四条决定成败的事实` → **五条**；第 1 条删掉"转 base 90°"改为**"必须站侧面"**并注明 `base` 限位只有 **±60°**
- `docs/texture-capture-guide.md` 三处：`base=90°` / "长边横过来 ×1.78" / "白卡隔离" **全部标为已推翻**（删除线 + 实测定论）
- `docs/decisions.md`：新增 **D58**（真机摆位：动关节改变不了分辨率；「自动分割出单块板」不可行）

### 性能与环境
- 标定搜索必须跑在**降采样图**（1/4 → 320×180）并**复用同一张画布**（每轮只 `rectangle` 清空 + `polygon` 绘制）；
  原尺寸下每次 `np.asarray` 就是 92 万元素，上万次迭代会卡死。
- `numpy 2.x` 移除了 `ndarray.ptp()` ⇒ 必须 `np.ptp(arr)`。

---

## §12 走 A 方案（物理靠近）时捅出来的四个更深问题（ADR D59）

用户拍板 **A：把机械臂挪近相机**。为把"挪一点→量一次"压成一条命令，
给 `tools/project_plates.py` 加了 `--capture`（开摄像头抓帧）+ 一句话 `CONCLUSION`
（报 `s`／大臂板 px／边缘过渡 `ramp`／投影是否出画）+ **搜索边界命中检测**。
**第一次跑就推翻了 D58 里当成真值的 `s = 3.80`。**

### ① ★ 旧标定 `s = 3.80` 是**搜索下界被截断**的值，不是测量结果

`s = 3.80` **恰好等于当时的 `S_LO = 3.8`** ⇒ 被区间截断，不是自由极值。
放宽到 `CALIB.s × 0.6~1.8 = 2.28~6.84` 后最优落到 **`s = 2.58`**。
⇒ 同一台机器、同一场景，"测量值"从 3.8 变 2.58 —— **这就是三块纹理全不合格的根因**。
**已加永久防护**：最优落在端点 2% 内就打印 `⚠️ s 落在搜索边界上 ⇒ 用 --s-range 放宽重跑`。

> **可复用铁律**：**任何带区间的搜索都必须显式检测"最优解是否贴边"。**
> 贴边 = 该值不是测量结果、是区间的人为截断。**比"分数不高"隐蔽得多** ——
> 分数看着还行、数值看着合理，实际毫无意义。

### ② 放宽区间后投影**依然对不上**

新最优 `s=2.58 / ox=660 / oy=445`，score 仅 **18.2**，叠加图**三个框都没贴板边**。
手动拨回旧标定附近（`s 3.4~5.0`、`ox=560`、`oy 600~690`）逐点扫，score 只有 **1.9~4.2**
（比 18.2 还差 4~9 倍）。⇒ **"缩放+平移"模型在本场景无法定位**：
- 板/立柱/底座/肩座**同一种黑且物理相连**，框落哪都在暗区；
- 三块板在 HOME 位**竖直首尾相接**（立柱 Z[38,60] 紧接大臂 Z[59.8,134.2]）⇒ **没有一条边能唯一定位**；
- **相机相对臂的真实位姿从未标定**，3 个自由度只有"三项乘积"一个标量约束。

### ③ 新发现：机械臂被画面**顶边裁掉**

行剖面：`y=0` 处有一条 **x[506,760] 连续暗带**，y=2 起才裂成多段 ⇒ 顶边切断。
**目标板不在画面内，四角必然缺角** ⇒ 无论 A 还是 B，**重新取景都是先决条件**。

### ④ ★ 改用「致动并观察」（motion-diff）—— 纯几何走不通时的通用突围手段

动一个关节 → 拍前后两帧 → 差分 ⇒ **差分区域 = 该关节及其下游零件**。
完全不依赖"相机在 −Y""板面平行于像平面"等任何假设。

| 动作 | 变化像素 | 占比 |
|---|---|---|
| shoulder 0.85→45 | 183097 | **19.87%** |
| elbow 112.62→141.8 | 131320 | **14.25%** |
| base 0→60 | 148177 | **16.08%** |

- ★ **物理臂确实在跟命令走** ⇒ 排除掉电/堵转/打滑。
  「MG90S 无位置回读」只是说**不能证明"到位"**，**不等于"没动"** —— 现在有正面证据了。
- 「随 shoulder 变 **且** 不随 elbow 变」筛出的区域 = **大臂板**
  （实测 x≈622–706、宽 84px）—— **这是纯几何做不到的归属判定**。
- ⚠️ 但**给不出干净单板四角**：肩座/平行杆/线缆/阴影连成一片，
  PCA 外接四边形出来的是整块区域（bbox 395×445）。
  ⇒ 适合**归属判定 + 粗定位**，精确四角仍需人工标 + 目视复核。

### 诊断脚本（`.workbuddy/captures/`，已 gitignore）
`diag_render.py` / `diag_az.py`（MuJoCo 离屏渲染探相机方位，**离屏渲染在本机可用**，
`mj.Renderer` 受 framebuffer 限制须 ≤320×180；`geom_matid` 不置 −1 时 `geom_rgba` 不生效）、
`joint_probe.py`（motion-diff 全流程）、`_grid.jpg` / `_zoom_home3x.jpg` / `_az_compare.png` /
`_parts_mask.png` / `motion/home*.jpg` `motion/{sh45,el141,b60}.jpg`。

### ★ 本轮方法论（跨项目可复用）
1. **区间搜索必查贴边**（① 的教训）。
2. **"测量值"与"边界截断值"长得一样**：发现两个相互矛盾的读数时，
   先查是不是区间设窄了，别急着调模型。
3. **纯几何推理走到尽头时，用"致动并观察"建立对应关系** —— 让系统自己动，
   差分就是最可靠的"哪个像素属于哪个部件"的答案。
4. **"无反馈"≠"没动作"**：没有位置回读不代表不能证明它动了，差分就是证据。

---

## §13 新机位后：窄带 PCA 定出四角，纹理首次落地（ADR D60）

用户按 D59 要求**重新取景并挪近** ⇒ 新机位里**整臂完整入画**（不再被顶边裁掉）、背景干净。

### ① 先纠一个误报：`ramp=10.78px 失焦` 是**假的**
`ramp` 是在**投影框边缘**测"暗↔亮"过渡的；旧标定定位本身就错（D59），框落在**暗区内部**
⇒ 边缘压根没有真实边界 ⇒ ramp 被拉长到 10px。放大目视：板边缘过渡约 **2px，并未失焦**。
⇒ **`ramp` 只有在四角正确时才有意义**，绝不能拿它反推"相机失焦 / 别再靠近"。

### ② 整臂剪影 IoU 配准（`fit_arm.py`）—— 失败，但失败得有信息量
用已知模型 + B 位姿态（`shoulder=45`）投影**整条臂剪影**，与实际剪影做 IoU：

| 版本 | 做法 | IoU | 结果 |
|---|---|---|---|
| v1 | (s, ox, oy) 全自由 | 36.3% | 最优解把模型推到 **`oy=830`（画面外）** —— 又是 D58 退化模式 |
| v2 | **bbox 中心强制对齐**，只搜 s | **25.3%** | s=3.50 / ox=540 / **oy=644**（与内置 CALIB 3.80/530/645 接近 ⇒ 交叉印证）|
| v3 | 再联合搜 (s, shoulder, elbow) | **30.2%** | s=4.35 / shoulder=41.5° / elbow=115.0° |

- **30.2% 是上限**：叠加图显示**模型的臂整体比实拍"高"** ⇒ 模型几何与 3D 打印实件存在
  **系统性差异**；同时反解出**物理臂实际角度与下发命令差 ~15°**（开环无回读的必然）。
- 踩坑：`table_top`（约 1m 见方）也在投影件里 ⇒ 剪影被撑成**全屏矩形**，
  IoU 退化成"实际面积占比"（19.3% ≈ 10069/57600）。**必须排除桌面。**

### ③ ★ 可用方案：窄带 PCA（`plates_corners.py`）
板本身是一条**直带** ⇒ 沿长轴开窄带（半宽 ~35px），**带内**暗像素 PCA 取主轴极值四角。
带外的一切（螺栓 / 立柱 / 平行杆 / 线缆）被**结构性排除** ——
这是绕开"整条臂是同一连通域"（D58 ③）的正解。
实测：大臂 **241×65px**（长宽比 3.69 / 真值 3.36）；小臂 **200×72px**（2.76 / 3.78）。
⚠️ 中心线端点仍是**目视读数**（±15px）⇒ 精度有限，但可复现、且比整图分割好得多。

### ④ 纹理首次落地（2 块；`make_texture.py --corners`，四角再向心收缩 8% 避边缘）
- `forearm_link` 18×68mm → 180×680px：✅ **干净可用**（黑底 + 两枚螺栓 + 镂空）
- `upper_arm_link` 22×74mm → 220×740px：⚠️ 中部**一条橙色线缆横穿**
⇒ **线缆无解**：实拍里线缆**悬在板前面**，只要框圈住板就必然含它；
向心收缩只能去**边缘**、去不掉**中间**。**软件层面没有办法。**
其余 7 块（`base_link`/`column_link`/`column_link_side`/`upper_arm_brace`/`forearm_brace`/
`tool_link`/`jaw_link`）在 B 位**被遮挡或不在视野**，需额外姿态/机位。

### ⑤ 环境事实
- MuJoCo 离屏渲染在本机**上限约 320×180**（640×360 会失败）。
- 渲染要看到颜色必须先 `m.geom_matid[i] = -1` 清材质，否则 `rgba` 被 material 覆盖成白。

### ★ 本轮方法论
1. **一个判据只有在其前提成立时才有意义**：`ramp` 依赖"框落在板边上"，框错了它就成了噪声
   —— 不要拿一个下游指标去反推上游前提。
2. **"模型对不上"要先怀疑模型，别一直调相机参数**：联合搜姿态后 IoU 只从 25%→30%，
   这就是"模型与实件有系统性差异"的证据。
3. **形状约束比位置约束强**：整臂轮廓 IoU ≫ 三个矩形框（后者欠定）。
4. **物理条件决定上限**：要拿到干净纹理，必须先满足
   "板完整入画 + 不被线缆横穿 + 目标面正对相机"，三条都是物理条件。
   ⚠️ ③ 已被本轮更正（线缆在板背面，见 §14）。

## §14 继续建模：揪出渲染纵横比 Bug；修正手性；四角改「投影初值 + 梯度吸附」

### 起因
用户选 A（拨线缆后重拍）→ 抓帧发现**线缆还在**。于是先试"软件去线缆"，
结果顺藤摸出一个**影响面很大的工具 Bug**。

### ★★ Bug：`mujoco.Renderer(model, height, width)`
参数顺序是 **(height, width)**，我全程按 `mj.Renderer(m, 640, 360)` 调用
⇒ 实得 **640 高 × 360 宽竖幅**，后续按横幅 `paste`/`resize` ⇒ **所有渲染对照图都拉伸/裁切**。
由此得出的**两条结论作废**：
1. "azimuth=270 与实拍一致（臂向右）" → 正确是 **azimuth=90**；此前模型是**镜像**的。
2. D60「模型的臂整体比实拍高一量级 ⇒ 模型几何与 3D 打印实件有系统差异」→ **渲染伪影，撤回**。
- **教训**：工具参数语义必须先验证。传反不报错、不告警，却让下游每条目视结论都错，
  而且错得"看起来很有道理"。
- 用 `enable_segmentation_rendering()` 拿 geom-id 图是**零歧义**的部件对应手段。

### 镜像的代价
整臂剪影 IoU：镜像下 36.3% / 25.3% / 30.2% → **修正手性后 43.2%**
（`s=4.05, θs=66.5°, θe=109.5°`）。**`s` 第三次独立吻合**（3.50 / 3.80 / 4.05）⇒ `s≈3.7–4.1` 可信。

### 线缆其实在板背面
4× 放大看清：橙色线缆在板**背面**，从**三角镂空透出**，并未挡板面
⇒ "线缆无解、必须拨开"**过强**；真问题是**四角把镂空圈进来了**。
（"高饱和=线缆"的初版判据也不成立：板近黑 V≈0.1，(8,7,23) 算出 S=0.70 —— **近黑区饱和度是噪声**。）

### ★ 新方法：投影初值 + 梯度吸附（`snap_quads.py`）
投影（IoU 43%，误差几十 px）+ 目视（±15px）都不够，但**合起来**够：
投影给"大致位置与走向"，沿四边采样梯度幅值取最大做吸附（edge-snapping）。
实测边分 8.78→**19.30** / 4.65→**18.74**，且**长宽比精确吻合真值**
（96.2×323.7=**3.37** vs 3.36；75.8×286.4=**3.78** vs 3.78）。

### 残留（未解决）
纹理一侧仍有大块**亮楔形**（镂空/背景）。根因是**语义不匹配**：
实板镂空、模型 `*_vis` 是**实心 box** ⇒ 需要**孔洞掩膜**或把镂空建成几何，调四角无用。

### 其他
- 试过的方向（均未成）：v1 用"饱和度"分离线缆（不可分）；v2/v3 部件归属集合运算
  （`changed()` 只给**运动包络**/月牙，不是整块零件；改交集式 `S_A∧S_B∧¬S_C` 后
  近枢轴处位移小被排除 ⇒ 区域被截断，测长宽比不可靠）。
- 本轮**未改动任何产品代码**（全部在 `.workbuddy/captures/` 诊断脚本里），
  只更新了 README §12 / ADR D61 / memory / skill。

## §15 A 落地：孔洞掩膜 —— 三层判据（连通性 / 形状 / 色差）

### 做了什么
用户选 A。给 `tools/make_texture.py` 加**填孔**（默认开，`--no-fill-holes` 可关）：
把纹理里的**非板面区域填成板面材质**，让"镂空实板"与"实心 box 模型"的语义一致。
新增 `fill_face_background()` + `_grow_mask()` / `_flood_bright_from_border()` / `_label_mask()`
（本机无 scipy/cv2，连通域手写）。复核图改成**三栏**：原图+四角 | 校正未填 | 填充后（黄=填入区）。

### 三层判据，每一层都是被实测逼出来的
| 层 | 判据 | 不加会怎样 |
|---|---|---|
| ① 连通性 | 亮区接触纹理边界 ⇒ 填；被板面完全包围 ⇒ 再按面积定 | 纯亮度会把**螺栓一起填掉** |
| ② 形状 | 保留的亮斑还须 bbox 填充率 ≥0.60 且长宽比 ≤2.0 | 只判面积 ⇒ 被暗区包围的**线缆小段**留下 |
| ③ 色差 | `R−B > 15` ⇒ 填 | 暗线缆**亮度低于阈值**（中位 32 vs 阈值 60）⇒ 只判亮度**永远去不掉** |

**填充色取实测板面中位色 `RGB(1,1,20)`** —— 近黑**带蓝**，不是纯黑；填 (0,0,0) 会有色差补丁。
（实测纹理亮度是**双峰**：板面峰 0~20、非板面峰 110~125，中间 30~90 是谷底 ⇒ 阈值取 60。）

### 结果
| 纹理 | 填入 | 暖色残留 | 亮(>60) 像素 |
|---|---|---|---|
| `upper_arm_link` | 33.4% | **0.00%** | 1.25% |
| `forearm_link` | 35.2% | **0.00%** | 0.85% |

纹理 = **近黑底 + 几枚螺栓亮点**，目视合格 ✓。`--selftest` 三项全过，其中第 ③ 项
"被板面包围的小亮斑必须**保留**"就是"判据是连通性而非亮度"的证据。

### ★ 新诊断判据：四边亮比例（"四角贴合吗"的定义本身就是它）
框精确贴合板 ⇒ 纹理四条边全落在板上 ⇒ 亮比例应为 0。实测
`upper_arm` 上 45.2 / 下 18.5 / 左 31.1 / 右 46.4%、`forearm` 上 57.6 / 下 33.7 / 左 61.5 / 右 19.7%
⇒ **框比板大，且两个框偏心方向相反**（一个偏右上、一个偏左）。

### ★ 教训：同一个数在不同前提下会有相反解读
我先加了"框外 / 镂空"二分统计（接触边界 vs 被包围），看到 `框外 29.1% / 镂空 0.0%`
就断言"框比板大 30%"。**但镂空若开口到板端，同样会接触纹理边界而被算进"框外"**
—— 两种解释都能套上这个数。于是换了一个**定义上无歧义**的判据（四边亮比例）。
⇒ **当两种解释都能套上同一个数时，该换判据，而不是换参数。**

### 判断：四角仍不完美，但**不阻塞**
纹理亮像素只占 **0.85~1.25%**，其余是均匀近黑 ⇒ 板面几乎不含空间信息，四角偏差主要影响
**螺栓位置的比例**。这也说明照片纹理的**边际收益主要在螺栓高光与表面细纹**，
与"直接给一个黑色材质"差别可能不大 ⇒ **下一步该先贴上去看效果**，而不是继续抠四角。

### 未解决
四角仍比板大（四边亮 18~62%），需要一条定义上无歧义的收紧方法（不能回到"调目标函数凑"，D59）。
其余 7 块板仍未拍。

### 其他
- 本轮改了**产品代码**（`tools/make_texture.py`）—— D60/D61 两轮"只改诊断脚本"之后第一次。
- 文档：README §12 第 5 条 + 当前状态 + 下一步 · ADR **D62** · memory · skill `arm-photo-texturing`。

---

## §16 A 方案收尾：前端接入照片纹理（ADR D63）—— 以及一条被 `flipY` 掀翻的推理

用户选 **A（前端渲染接入）**。目标是把已出的 2 块纹理按面材质数组接进 three.js，**看实际效果**。

### 实现（4 处改动）

| 文件 | 改动 |
|---|---|
| `config/robot.yaml` | `upper_arm_link` / `forearm_link` 的 `geometry` 与 `details[0]` 各加 `texture: mearm/tiles/<id>.png`（**纯外观字段**） |
| `robot/model/Link.ts` | `PlateGeometry` 加 `texture? / textureFlipU? / textureFlipV?` + `plateTexture()` 归一化 |
| `robot/model/textureRegistry.ts`（新） | `import.meta.glob('../../../../assets/textures/**/*.png', {eager,query:'?url'})` 静态登记 key→URL；查不到返回 `undefined` |
| `components/RobotScene/buildRobotObject3D.ts` | plate 有纹理时返回 **6 元材质数组**：大面贴照片、其余四面板色 |
| `components/RobotScene/TestProbe.tsx` | 新增 `setCamera(pos,target)`（DEV 守卫，服务视觉验收截图） |

**为什么必须按面**：实测 `RoundedBoxGeometry` 的 UV 是「**每个面各自铺满 [0,1]**」，
一个材质会让 5mm 窄边也被整张照片铺满 ⇒ 侧面出现拉扁的螺栓，比不贴更假。
大面 = `size` 里**最小那一维**所在的轴（大臂 `[22,5,74]` ⇒ ±Y ⇒ group 2/3）。

### ★ 实测 UV 表（`.workbuddy/captures/uv_probe.mjs`）
| group | 法向 | Δu | Δv |
|---|---|---|---|
| 0 | +X | `-Z` | `+Y` |
| 1 | −X | `+Z` | `+Y` |
| **2** | **+Y** | `+X` | **`-Z`** |
| **3** | **−Y** | `+X` | **`+Z`** |
| 4 | +Z | `+X` | `+Y` |
| 5 | −Z | `-X` | `+Y` |

⚠️ 顺带实测：**`RoundedBoxGeometry` 是「非索引几何」**（`g.index === null`），group 的
`start/count` 是**顶点范围**而非索引范围 —— 按索引遍历会直接 `TypeError`。
两个大面的 Δu/Δv **必有一个相反**（盒体展开的必然）⇒ 必须有一面镜像，否则两侧总有一侧反。

### ★★ 踩到的坑：`TextureLoader` 的 **`flipY` 默认 `true`**
我据上表推「面 3 (−Y) 需要翻 v」，实现后**实测完全相反**。根因：
**three 上传纹理时默认垂直翻转 ⇒ 图像顶行 ⇔ `uv_v = 1`（不是 0）**。
漏掉这一条会让**所有 v 方向的推理整体反号**。修正后「基准面 = + 轴侧」需镜像
（面 2 翻 v、面 3 原样）⇒ 与实测一致。**这是个不报错、类型检查也查不出的隐式默认值。**

### ★ 怎么定案的：四象限**探针纹理**受控实验
拿真实纹理比"螺栓该在哪"**定不下来**：真实纹理是近黑工程塑料，特征只有"螺栓亮点 + 镂空暗缝"，
而模型螺栓位置（端盖在中线）与实物（有偏移）本就不同 ⇒ 读数在 0.13~0.55 之间飘。
改用：把纹理**临时替换**成「四象限纯色 + 对角白带」，同机位各拍一张，读完**还原**
（`git status assets/` 复核干净）。

| 视角 | 期望（左上/右上/左下/右下） | 实测 |
|---|---|---|
| 相机在 **−Y**（看面 3） | 红 / 绿 / 黄 / 蓝 | ✅ |
| 相机在 **+Y**（看面 2） | 绿 / 红 / 蓝 / 黄 | ✅ |

⇒ **两侧都对，且互为镜像**（真实物体两面的关系）。
又一条 D61 一 的同款教训：**目视与推断都不可靠，受控实验才算证据。**

### 工程守卫
- **无 DOM 不加载纹理**（`CAN_LOAD_TEXTURE`）：`TextureLoader` 内部走 `document.createElementNS`，
  而测试跑在 **node 环境** ⇒ 不加守卫会直接 `ReferenceError: document is not defined`，**5 个测试挂掉**。
- **key 未登记 ⇒ `console.warn` + 回退纯色**：贴图失败是**静默**的。
- `tests/unit/plateTexture.test.ts`（7 项）：含**纹理长宽比必须与板大面一致**（抓配错图）
  与"声明的 key 必须真的被登记"。

### 验收（全绿）
| 项 | 结果 |
|---|---|
| `tsc -b --force` | 0 error |
| `vitest run` | **300 passed**（新增 7） |
| `vite build` | ✓ 775ms；`upper_arm_link` 75.42kB / `forearm_link` 50.13kB **进产物** |
| 产物 JS/HTML/CSS 中 `__armPilot` | **0 命中**（仅 sourcemap 含，符合既有约定） |
| `pytest tests/sim -q` | **147 passed** |
| `ui-smoke.mjs` | **85/87 PASS**（2 FAIL 见下） |

### ⚠️ 2 个 e2e FAIL 是**环境差异**，不是回归
`Phase 8：连着后端（末端非 serial）点 Real Robot → 拒绝切换` 与 `拒绝原因必须点名链路末端` 失败，
实测 `simActive=false realActive=true` ⇒ 按钮**真的切到了 Real**。
根因：**残留的真机后端仍在 8090 监听**（pid 23192，`netstat` 复核；e2e 自己也报了
`后端为复用实例（非本脚本拉起）`）⇒ `device === 'serial'` ⇒ 安全门**正确地放行**了。
同一批里 Phase 10.6 的同类断言（"未连接"场景）**PASS** ⇒ 与本次改动无关（本轮只动外观层）。
⇒ 清掉该后端重跑即可恢复 87/87（**该进程归用户裁决，未擅自杀**）。

### 效果观察（用户最关心的那一条）
大臂 / 小臂由亮蓝（`#4d8fe8` / `#6aa0ff`）变成**近黑 + 几枚螺栓亮点**，
螺栓位置与纹理原图吻合（上螺栓 u≈0.51 vs 纹理 0.545）⇒ 朝向正确。
**结论：形体更像真机了，但信息量很低**（可见内容主要就是那几枚螺栓）
⇒ 若要更"像"，下一步该动的不是纹理而是**光照 / 材质**。

### 未解决
- 其余 **7 块板**未拍（需额外姿态 / 机位）；
- 四角仍比板大（四边亮 18~62%），需要定义上无歧义的收紧方法；
- 近黑纹理细节在深色光照下偏暗。

---

## §17 选项 C 落地：暗端材质与光照（ADR D64 / D65，✅ 已完成）

### 结论：板面"看不见"**不是偏暗，是信息被 8bit 量化抹掉**

- 实拍按白桌曝光 ⇒ 黑亚克力落在 **1~3 码值**。tile 实测（全图口径）：
  `upper_arm_link` `≤3` 占 **72.2%**、`forearm_link` **77.0%**；`L<40` 的像素**只用到 41 档码值**。
  经 three 的 sRGB→线性（`sRGB(2) → 0.0006`）再乘 <1 辐照度 ⇒ 渲染 `L≈0.08`。
- **等比例提亮救不回来**（受控实验 `tile_lift_preview.png` 四曲线）：
  线性 ×8 只放大 JPEG 块；gamma 1/2.2 压成近平色；推 `L≈60` 需 **+6.2 EV**，
  而那时板面摊成 `41/60/74` **三级平台** ⇒ 假色斑，**比纯黑更假**。⇒ `exposureEv` 保持 **0**。
- **形状可读性来自镜面项**：非金属 `F0≈0.04`，镜面项**不乘**那个 ≈0 的暗反照率 ⇒ 走 IBL。

### ★ 最大的坑：`scene.environment` 下 `material.envMapIntensity` 被 three **覆盖**

- 症状：V5/V6 统计**逐位相同**（`L=59.17 / p05=57.36 / max=78.2`）—— yaml 改了却毫无影响。
- 根因：three `0.186.0` `WebGLRenderer.js:2736`
  `if (material.isMeshStandardMaterial && material.envMap === null && scene.environment !== null)
   m_uniforms.envMapIntensity.value = scene.environmentIntensity;`
  ⇒ **只有材质自带 `envMap` 时它的 `envMapIntensity` 才被尊重。**
- 这个坑是被一个 **TS 层级错误**顺带暴露的：`environmentIntensity` 写错层级 ⇒ `undefined` ⇒ 等效 `1.0`
  ⇒ 恰好解释了 `59.17` 这个"好看得过分"的数。**类型检查抓住了一个物理量级的错误。**
- 解法：`RobotScene` 的 `SceneEnvironment` **整体撤除** ⇒ 改为**逐材质 `envMap`**
  （`plateEnvironment.ts` 程序化 `RoomEnvironment` + PMREM，**零外部 HDR 资产**）。

### 实测（同机位、ROI `x[402,520) y[380,640)`，n=30680 px）

| 量 | 改前 | 改后 | 变化 |
|---|---|---|---|
| `L` mean | 0.08 | **12.21** | ×152.6 |
| **`hp_std`（细节能量·主判据）** | 0.086 | **0.293** | **×3.41** |
| `p95−p05` | 0.07 | 1.86 | ×26.6 |
| `L≥20` | 0.0% | 0.0% | 仍**暗于背景 22.7** ⇒ 剪影保留 |

**零外溢**（按基线图的底座蓝板像素集合逐像素比对，46928 px）：
`50.637 → 50.637`，**×1.0000、`max|Δ|=0.00`**。对照：`scene.environment` 方案 ×3.3757（V6）/ ×2.6297（V1）。
**"零外溢"是逐像素事实，不是"看着差不多"。**

**旋钮性质**：`environmentIntensity` 与 `roughness` **只缩放整体亮度、不改变细节能量**
（`L` 动 4 倍时 `hp_std` 只动 11%）⇒ "再亮一点"换不来更多细节。

### 收尾参数（`config/robot.yaml → appearance.texturedPlate`）

`environmentIntensity: 0.25`（板面 12.21 < 背景 22.7 ⇒ 剪影不糊）· `exposureEv: 0` ·
`roughness: 0.6` · `metalness: 0`。
`git diff --numstat config/robot.yaml` = **`+36 −0`** ⇒ 运动学层一行未动（D55）。

### ★ 量测纪律：固定 ROI 只在**同一机位**下可比（本轮又踩一次）

- `light_v7_home.png` 是**等轴测机位**，我拿前视的固定 ROI 去量它 ⇒ 读出
  `L=27.68 / hp_std=5.37 / 泄漏 25.51%`，**三个数全是假的**（ROI 大部分落在背景网格上）。
- `measure_base.py` 第一版按"强蓝像素 **bbox** 画矩形" ⇒ 矩形横扫下半幅，混进**确实该变**的
  贴图黑板与 UI 面板 ⇒ 假 `×1.0546`。正解：**用基线图算像素集合、在固定集合上量**（+3×3 腐蚀去抗锯齿边）。
- 教训：**换机位必须重定 ROI，且必须目视复核 ROI 是否真的落在物体上**（D39/D40）。

### ★ 验收纪律（ADR D65）

- **端口预检曾是「恒假」的**：`netstat -ano | grep -E "LISTENING" | grep -E ":(5173|…)\b"`
  —— 两个独立缺陷叠加：① **漏 `-E`** ⇒ BRE 里 `(` `|` `)` 是字面字符 ⇒ **永不可能命中**；
  ② 补 `-E` 后，**`\b` 紧跟分组右括号 `)`** 在本机 **GNU grep 3.0** 下失效（`(8090)\b` 不命中，`8090\b` 命中）。
  ⇒ 它在 8090 **确实在监听**时打印"无残留监听"。**恒假的检查比没有检查更危险。**
- 可靠写法（不依赖正则语义）：`netstat -ano | tr -d '\r' | awk '$4=="LISTENING"{n=split($2,p,":"); if (p[n]==8090) print $5}'`
- `taskkill //PID` 在本机 Git Bash 下报 `无效参数/选项 //PID`（`//` 没被转成 `/`）且**静默失败** ——
  我第一轮"杀掉 dev server"其实没杀掉。改用 PowerShell `Stop-Process`，再用 awk 复查。
- **`ui-smoke.mjs` 新增 `skip()` 语义**：复用 8090 上的既有实例时若 `device=serial`，
  「末端非 serial ⇒ 拒绝切换」前提不成立 ⇒ 报 `[SKIP]`（不计 PASS/FAIL）并打 3 行横幅明说"环境差异非回归"。
  **两路受控验证**：干净环境 `88/88 PASS, 0 FAIL, 0 SKIP`；桩（`stub_serial_backend.mjs` 谎报 `device=serial`）
  ⇒ 那两条转 `[SKIP]`（total `87 → 85`）。桩只答 `/healthz`：真起 `config.serial.yaml` 会**打开串口**，
  机械臂若连着会产生**实际硬件副作用**。

### 本轮遗留

- 残留真机后端（pid 23192）**已由我杀掉**（上一条记录写的"未擅自杀"已不再成立）；
  e2e 现在会主动提示怎么拿到完整结果。
- ⚠️ **上限到此为止**：本节所有手段都只是"把已经存在的镜面信息显示出来"。
  板面在 tile 里只用到 41 档码值，**真正能把细节送进数据的是拍摄端**。
- 下一步仅剩 **A. 补拍其余 7 块板（必须同时解决黑件暗端曝光）** / **B. 修四角**。


---

## 夹爪（jaw）按实拍照片反解重构 + e2e 探针两类静默失真（ADR D66 / D67）

### 一、用户请求

> 爪子也按照这些图片进行重构（附 5 张实拍近景）

### 二、做了什么：把 `plate` 占位换成 `type: jaw` 参数化平面轮廓

| 层 | 文件 | 内容 |
|---|---|---|
| 真值 | `config/robot.yaml` | `jaw_link.geometry: type: jaw` + 14 个形状字段（`length/thickness/width/tipWidth/gearRadius/gearTeeth/toothDepth/serrations/serrationDepth/serrationSpan/tipSkew/neckInset/neckAt/holeRadius/color`） |
| 类型 | `Link.ts` | 新增 `JawGeometry` / `JawRenderSpec` / `jawRenderSpec()` / `DEFAULT_JAW_SPEC`；**删掉** `DEFAULT_JAW_SIZE` / `jawPlateSize()` |
| 解析 | `loadRobotModel.ts` | `GEOMETRY_TYPES` 增 `'jaw'` + `case 'jaw'`；**前置自洽校验** `pitchRadius < width/2` ⇒ 抛 `RobotConfigError` |
| 渲染 | `buildRobotObject3D.ts` | `buildJawOutline()`（一条连续闭合折线）+ `JAW_PLANE_MATRIX`（循环置换，行列式 +1）+ `createJawGeometry(spec, mirrored)` + 重写 `createGripperJaws()` |
| 单测 | `linkGeometry.test.ts` | 开合判据改为**读实际几何顶点**（爪尖 = Z 最大处的平均 Y），不在测试里重算符号 |

**三条派生关系在渲染层推、yaml 不配平**（否则就是把"配平"推给配置文件）：
① 两齿轮中心距 = `2 × pitchRadius`（标准啮合）；② 爪指中线内偏 `δ = pitchRadius − width/2`
（齿轮中心落在爪指**内侧缘**上）；③ 由 ①② 得 **`θ=0` 时两爪内侧缘正好贴合**。
数值：`pitchRadius=10.5` `gearRootRadius=9` `fingerInset=6` `hubOffsetY=10.5` 齿弧内齿数 11。

### 三、踩到并修掉的具体坑

- **`ExtrudeGeometry` 不做布尔并集** ⇒ 齿轮盘 + 爪指必须是**一条** Shape，两个 Shape 会
  z-fighting / 露接缝。
- **镜像必须反转点序**：轮廓 x 取反后绕向变顺 ⇒ 挤出体法线朝里；`shape.holes` 也必须与外轮廓反向。
- **`TS2366`**：`LinkGeometry` 增 `jaw` 后 `createGeometryObject` 的 switch 不穷尽 ⇒ `case 'none': case 'jaw': return null;`。
- **`RobotConfigError: geometry.type 非法: jaw`**（16 个单测连锁挂）：解析层白名单没同步。
- **单测手算爪尖位置错了 21**（漏了 `JAW_PLANE_MATRIX` 的置换，取到了 Y 方向的齿轮中心偏移）
  ⇒ 改为**直接读 geometry 顶点**。
- **板边 0.3mm 倒角让爪尖偏 0.176mm**（几何事实）⇒ 贴合用 0.5 容差，另加**精确**断言 `closedLeft.y ≈ −closedRight.y`。
- **Python 归一化后 `np.clip(a,0,255)` 把整幅图压成黑**（连续 4 张全黑，一度误判"裁切位置错"）
  ⇒ 正解 `np.clip(np.power(a,gamma)*bright*255.0, 0, 255)`。
- **`readCommandCell` 的全局选择器 `.sidebar table.grid tbody tr` 命中两张表** ⇒ 见下。

### 四、e2e 出现 6 FAIL：**5 个与爪子无关**

根因是 `start.bat` 残留的**一对**进程：后端 `config.serial.yaml`（8090，device=**serial**）
+ **带 `VITE_AUTO_CONNECT=ws` 的 vite**（5273）⇒ 页面**加载即自动连上真机链路**，
Actual 改由 WS 回推驱动 ⇒ Mock 闭环类断言整体失真。**清干净后 88/88。**

| FAIL 读数 | 真因 |
|---|---|
| 状态表 Command 列跟随滑杆 — `0 / 0` | 探针**读错了表** |
| 幽灵分离 `0.2mm` / 健康"已到位"(0.18°) | 滞后读数**拆成多次 CDP 往返** ⇒ 读到已收敛值 |
| 松手后收敛 / TCP 误差归零 | 被残留后端接管 |
| Phase 10.6 点 Real Robot 未被拒绝 | 同上 + **mode 跨批次残留** |

**探针修复（受控实验给出的证据）**：

`.workbuddy/captures/probe_table_scope.mjs` —— 同页面只翻转"是否已发过命令"：

| 组 | 自变量 | `table.grid` 数 | 旧选择器 `rows[1]` | 新选择器 `rows[1]` |
|---|---|---|---|---|
| A | 未连接 | 3 | `0.8°`（J2） | `0.8°` |
| B | 已连接、**未**发过命令 | 3 | `0.8°` | `0.8°` |
| C | 已连接、**已**发过命令 | **4** | **`0 / 0`** ← 丢帧/拒绝 | **`39.9°`** ← J2 |

（`transportStats` 只在**帧流过之后**才非 null ⇒ "指标 / 值"表此时才渲染；B 组正是关键对照。）
修法：与 `readJointGap` 一致先 `cardByTitle('状态 · Status')` 再取 `tbody tr`。
⚠️ **必须放在函数里**：模块级模板字面量会在 import 时求值，而那时 `cardByTitle` 还在 TDZ
⇒ `ReferenceError: Cannot access 'cardByTitle' before initialization`。

滞后相位四条断言合并成单个 `readLagSnapshot()`（一次返回
`{command, actual, gap, errDeg, errVals, ghostGap, health}`）。**自洽性证据**：
修前 (b) 39.1° 却 (b1) 只分离 **0.2mm**（自相矛盾）；修后 **53.5mm / −39.06°**（三读数互相印证）。

Phase 10.6 补齐 D65 漏掉的一处：`device === 'serial'` 时那两条改报 `skip()`；
并**入口显式归位 Simulation**（污染环境出现过"提示条说已拒绝、按钮仍高亮 Real"的自相矛盾 ——
真因是 Phase 8 走 serial 分支时末尾"归位"不执行，mode 被留在 real）。

### 五、工具事实（本轮新增，跨会话复用）

- **`(cmd &)` 起的后台进程只活到本次工具调用结束** ⇒ "先起 dev server、下一条命令再跑 e2e"
  必然 `ERR_CONNECTION_REFUSED`；**起服务与跑 e2e 必须在同一次调用里**。
- **`/tmp/xxx.log` 重定向在沙箱内会被拦**（日志文件不生成）⇒ 日志落到工作区内（已 gitignore 的目录）。
- `taskkill //F //PID` 报 `无效参数/选项 - '//F'` 且**静默失败**；**`taskkill -F -PID`（单横线）可用**
  （D65 记的是 `//PID` 的同类问题，今天 `//F` 再次复现）。
- `node "/e/cnb/..."` 会被解析成 `E:\e\cnb\...` ⇒ 传 **`E:/cnb/...`** 正斜杠盘符形式。

### 六、验收四件套（全通过）

| 判据 | 结果 |
|---|---|
| `tsc -b --force` | **0 error** |
| `vitest run` | **313/313**（24 文件） |
| `vite build` + `grep -o "__armPilot" dist/assets/*.js \| wc -l` | 构建通过；**0 命中** |
| e2e `ui-smoke.mjs`（干净环境） | **88/88 PASS, 0 FAIL, 0 SKIP** |

### 七、本轮遗留

- `serrations: 9` / `serrationDepth: 0.75` / `color: "#2b323c"` 是**观感目视调参、未做受控量测**
  （不要与"从照片读轮廓"——有剪影证据——混为一谈）。
- 未跑污染环境的**完整**回归（serial 后端不在手边，无法复现 6 FAIL；已用受控实验 + 干净环境两路替代）。
- 下一步仍只剩 **A. 补拍其余 6 块板**（`jaw_link` 已由 D66 移出）/ **B. 修四角**。

## 18:0x · 视口灰底 + 件色近黑 + 世界轴默认关（ADR D68）

用户三条诉求，全部属**外观层**（D55），互不牵连：

1. **视口背景** `#14171c` → `#8b8e93`；★ **Z=0 参考圆盘必须跟着改**
   （`#1d222a` → `#7a7e84`）—— 否则灰底上留一个近黑"黑洞"。
   网格**未动**：灰度自动反相（采样 线 111,116,123 < 背景 139,142,147）仍可读。
2. **板件蓝 → 近黑** `#232830`（6 个值、11 处）。**先验证再动手**：贴图件两个大面用的是
   `createTextureMaterial` 的**白色基色**（"照片已含颜色，避免二次染色"），
   `geometry.color` 只在**非贴图的 4 个窄边**上生效 ⇒ 改 color **不会**把照片一起乘黑。
   未改端盖 `#2b3446` / 舵机 `#2f3644` / 螺栓 `#161a21`（非板的深色机加工件）。
3. **世界轴默认关**（`showWorldAxes: false`，开关保留）。"移动后中轴那根不动的蓝线" =
   `axesHelper` 的 **Z 蓝轴**，它与 **J1 轴重合**（所以看着像"机械臂中轴上的蓝线"），
   J1 一转立柱让开它就露出且不跟着动。
   **受控实验**（只翻 "World Axis" 一个开关）+ 像素 diff：关掉后 3D 里消失的**只有 X/Y 两条长斜线**；
   `y[80,120)` 那处差异经可视化确认是**复选框自身的勾选标记**；
   ★ **立柱上那道亮蓝竖条在 diff 里没有任何标记** ⇒ 它是 `#4d8fe8` 侧板（本轮已改黑），
   **不是轴** —— 这一条排除了"看着像轴的蓝色竖条"，避免改错对象。

未改：DragHandle 把手球 `#4c8dff`、TCP 标记 Z 短柱 `#6699ff` —— 交互 / 调试件，不是模型件
（把手球的蓝 / 红还带"可达 / 超程"语义）。

四件套：tsc 0 error · vitest 313/313 · build 通过 + `__armPilot` 0 命中 · e2e 88/88 PASS, 0 FAIL。

★ **工具坑（新，会骗人）**：`curl http://127.0.0.1:8090/healthz` 返回
  `upstream connect failed` **502** 时，先怀疑**本机代理软件**（`env` 里没有 proxy，
  是 curl 读了系统代理设置）。判据：`netstat` 无监听 **且** `curl --noproxy '*'` 直接
  `exit 7` ⇒ 端口是**空的**，那个 502 是代理给的，不是"有个坏服务占着"。
  否则会把"端口空闲"误读成"环境污染"，白白绕路。
★ **诊断脚本升级**：`shot_front.mjs` 新增通用 `TOGGLE_OFF="World Axis,Actual Arm"`
  （按 label 文本点掉复选框），`HIDE_GHOST=1` 降级为它的特例
  ⇒ "只翻一个开关"的受控实验从此是一条命令。

**腕端「空洞」= 小臂侧板缺 12.5mm 料（ADR D69）**

用户：机械臂上"连接前爪部分"有一段空洞，"直接延伸填充"，成因是电机贴纸反光导致之前未捕捉到。

★ **定位法（可复用）**：不从截图猜，把 forearm_link 局部 z 上每个件的覆盖区间列出来 ——
  侧板 0..68 / 端盖 −2..2 / 横撑 61.5..66.5 / **空缺 68..80**；腕点 = length = 80、
  tool 板从 80.5 起 ⇒ 12.5mm 完全无料、侧视看穿到背景。洞的**宽度与归属一次算清**。

★ **修法**：主板不动（贴图契约逐字保留），另加两片**无贴图** `box [18,5,12] @ z=74`。
  不要"延长主板" —— 会触发 plateTexture.test.ts 的纹理长宽比契约（tile 3.778 vs 板 4.444），
  而延伸段本来就没照片数据，硬套贴图本身就是错的。

★★ **两条只在"插入位置"上暴露的耦合（血泪）** —— `tools/make_texture.py::load_plate_sizes()`：
  · 它枚举的是 `geometry`(plate) **+ details 里的所有 plate** ⇒ 补料用 plate 会混进"待拍板"清单；
  · 别名键是 **`enumerate(details)` 的下标**（`forearm_brace` = forearm_link 的 index 3）
    ⇒ 插在中间会把后续下标整体挤错，测试当场报
    「采集指南提到脚本不认识的文件名：['forearm_brace']」。
  ⇒ 定案：**用 `box`（不在枚举范围内）+ 追加到 details 末尾**。清单逐字恢复原样（已打印核对）。

★ **四件套漏 pytest**：D66 的两处漂移就是这么漏掉的 —— MJCF 生成器不认 `type: jaw`
  （已与前端 `case 'none': case 'jaw'` 对齐）、指南里 `jaw_link.jpg` 这个**字面**被测试的
  `\`name.jpg\`` 正则抓到（已改写成不含该字面的说法）。
  ⇒ **跨端改动（config / geometry 类型）必须跑五个**：tsc · vitest · build(+探针扫描) · e2e · **pytest**。

验收：tsc 0 error · vitest 313/313 · build 通过 + `__armPilot` 0 命中 · **pytest 147 passed** ·
e2e 88/88 · 像素 diff 23657px **全部**落在缺口处、画面其余 0 变化。

**爪角「随前后移动一起变」= 模型缺腕关节（未定案，停在判据阶段）**

用户报："爪部分角度会随着前后移动同时有变化"，附 5 张高清实拍（2133x1600）。

★ **机理（代码逐值确证，不是渲染 bug）**：
  · `tool` 是 `fixed`、`origin.rotation = 0`；`buildRobotObject3D.ts` 的 `JAW_PLANE_MATRIX`
    把爪轮廓的**长度轴映射到工具系 +Z** ⇒ **爪的世界倾角 ≡ 小臂绝对倾角**，爪自身无自由度。
  · `elbow` 存绝对角（coupling gain −1 把肩角抵消）⇒ 爪倾角 = `elbow − 90`，与肩角无关。
  · 受控验证（`captures/probe_jaw_move.mjs` 的 `j=` 直设关节模式）：
    `jEl112`(sh 0.9/el 112.44) 与 `jSh30`(sh 29.9/el 112.44) 渲染出的远端**倾角完全一致**、只是平移。
  · 后果：IK 在矢状面内只有 2 个自由度 ⇒ 改 TCP 位置**必然**改 elbow ⇒ 爪必然跟着转。
    可达域内实测：home(112,93.8) el 112.6； (90,60) el 131.8； (130,80) el 118.0； (130,100) el 108.6
    ⇒ 前后移动一趟，爪摆动 ≈ 23°。

★ **实拍侧唯一可信的一条量测（p5，臂抬起位）**：
  `p5_three_lines.png`：小臂两条长条上缘 **+40.8° / +26.4°**，爪指上缘 **+14.2°**
  ⇒ **腕处存在 12°~27° 的折角，而模型写死 0°**；折角处正对着一颗亮轴销。
  另：p1~p5 的**爪指绝对倾角**看起来都落在 +7…+20°（位姿差异极大却几乎不变）
  ⇒ 疑为**被动腕连杆把爪的绝对角锁住**（保水平），这与"4 舵机的真机爪=刚接小臂"的默认假设冲突。

★★ **实拍量化踩过的三个坑（血泪，已写进 skill）**：
  ① 机位不固定 ⇒ 图像里的角度不可跨图比；只有**同机位扫描**（w2_S7 / w2_S8，1280x720）才行；
  ② 机械臂投在**白布上的影子**在灰度 70 以下 ⇒ 暗阈值必须 ≤60，否则 PCA 被拖到竖直（实测 +82°）；
  ③ 爪根后面 35px 就是**竖直的 S6 舵机块** ⇒ 取"最右侧 16%"会把舵机并进 PCA（量出 +63°）。
  ⇒ 正解：**逐列拆暗段**取上缘 + Theil–Sen 中位斜率，或按"每条暗段分别跟踪"。

★ **未定案的两条分支**（决定修法，不能直接动手）：
  A. 折角**固定** ⇒ 改 `tool.origin.rotation` + 重拟 homePose（动标定，需 ADR）；
  B. 折角**由连杆驱动**（爪绝对角恒定）⇒ 需新增被动 `wrist` 关节
     （`coupling: {joint: elbow, gain: -1}`）。**注意**：这会让 elbow→TCP 不再是一条直线，
     `ik.ts` 的平面 2R 前提失效（L2 不再固定），必须改 IK + 重标定 + 全五件套。
★ **收口判据（5 分钟可做）**：固定机位 + 画面里放一个水平基准，**只扫 S8**（30/45/60/75/90/100）
  各拍一张 → 量"爪指倾角 vs 小臂倾角"随 S8 的变化：同步变 ⇒ A；不变 ⇒ B。

## 爪角随前后移动变化 —— **B（被动腕连杆）已被受控实验证实**（2026-09-13 晚）

用户选 B 后，先补做了**决定性受控实验**，再谈改动。结论：**B 成立，现行模型（折角恒为 0）被否决。**

### 实验（数据仓库里已有：定机位 S8 扫描 `.workbuddy/captures/w2_S8/`）
- 相机固定（背景逐像素同一场景；立柱在画面里竖直 ⇒ **滚转 ≈0**）
  ⇒ **两帧同一构件的画面倾角之差 = 其世界倾角之差**，不需要标定相机。
- 只扫 S8：小臂绝对角 137.68° → 108.44°（世界下倾 47.68° → 18.44°），**Δ = −29.24°**。

| 帧 | S8 | 小臂绝对角 | 小臂世界下倾 | **爪指画面倾角（实测）** |
|---|---|---|---|---|
| S8_030 | 30 | 137.68° | 47.68° | **+1.27°** |
| S8_100 | 100 | 108.44° | 18.44° | **+8.58°** |

- **Δ爪 = +7.31°**，而 Δ小臂 = +29.24° ⇒ 折角变化 = **−21.93°**（折角随姿态变）
- ⇒ 爪的**绝对倾角近似恒定（≈水平）**：折角正好抵消小臂摆动 = 被动腕连杆的特征。
- ⇒ 现行模型会把爪的倾角绑死在小臂上（预测 Δ爪 = 29.24°），**被实测否决**。
- 复核图：`.workbuddy/captures/jawangle/sweep/_jaw_axis_cmp.png`（轴线画回原图目视复核过）
- 脚本：`judge_jaw.py`（自动，失败）/ `draw_jaw_axis.py`（**人工选端点 + 画回原图**，采信用这个）

### 量测方法论（本轮新踩的坑，已写进 skill）
1. **"最右端暗像素"永远是桌面**：桌面黑带横贯画面下部，且**不与下边界相连**
   （最下几行是亮地垫）⇒ "踢掉触底连通域"无效（种子仍落在 x=1274 的右下角暗块）。
   "踢掉碰任意边界的连通域"也没用——机械臂坐在桌面上，臂+桌面是**同一个连通域**。
2. **自动脊线跟踪（逐列取最近暗游程质心）方向是对的，但种子一错全错**。
   判据不稳时**退回人工**：4× 放大 + 20px 网格 → 目視选钳口根部与爪尖中点 → 算角 → 画回原图复核。
   两帧的端点选点误差 ±4°，但**效应量 29°**，足以定性。
3. 钳口"轴"要取**两片爪尖的中点**（两片张开，取单侧会带上开合角）。

### 锁死角的取值
实测 +1.27° / +8.58°（均值 ≈ +4.9° 下垂）。取模型约定（离天顶）即 ≈ **94.9°**。
**决定用 90°（严格水平）**，理由与 elbow 取理论 −1 而非拟合 −0.81 一致：
平行四边形杆件平行于底盘 ⇒ 理论值就是水平；实测残差 ≈5° 落在选点误差内，先记下待细测。

### 关键自洽性检验（重要，说明不用重拟 homePose）
被动腕（锁 90°）+ 目标点减去爪的水平前伸 40mm 后做 2R：
- L1 = 肩→肘 = 80、L2a = 肘→腕 = 80（**不再是 120**）
- home 的 TCP (115.05, 0, 109.23)，相对肩 (0,60) 的 dr/dz = (75.05, 49.23)
- ⇒ α = 111.7°、φ = 56.72° ⇒ **θs = 0.84°、θe = 112.54°**
- 与 robot.yaml 的 homePose（shoulder 0.8499 / elbow 112.6186）**逐值吻合**
⇒ **homePose 与四个舵机标定全部不用动**，只多一个被动腕。

### 待办（动手前已列清，跨 3 个消费端）
前端 model/FK/**IK 必须重写**（平面 2R 的 L2 假设失效）/渲染 + Go `JointOrder` 跳过规则
+ MuJoCo（被动关节要生成 hinge + `<equality><tendon>` 三关节约束）+ 测试 + 文档。


### 被动腕落地（一次做全 · 六套验收全绿）✅

按用户选的「一次做全」把 `tool` 改成 `passive` 并同步**全部六个消费端**：

| 层 | 改动 |
|---|---|
| `config/robot.yaml` | `tool: type: passive` + `coupling{joint: elbow, gain: -1}` + `limit{min:90,max:90}` |
| 前端模型层 | `Joint`/`RobotModel`/`loadRobotModel` 支持 `passive`；`isMovableJoint` 收窄为 `revolute && max>min` |
| 前端 IK | 整文件重写：2R 作用对象改为「肘枢轴→腕枢轴」，新增 `wristId` / `toolOffset` / `PROBE_TOL_MM` / `assertPlanarWrist` / `requireConstantToolOffset` / `wristSagittal` |
| Go | `Load()` 与 `JointOrder()` 过滤 `Type != "revolute"` |
| MuJoCo | 生成 `<tendon><fixed>`(shoulder+elbow+tool) + `<equality><tendon>` 钉 90°；`model.py` 新增 `dof_vector()`/`ctrl_vector()`/`constraint_torque()` 修正**状态帧错位** |
| 配置物理 | `physics.yaml` 台面顶 30 → **46mm**（爪锁平后包络最低 15.8→32.73mm，旧台面够不着） |

#### 本轮最值钱的三条教训

1. **`qpos` 坐标数 ≠ 自由度**：被动腕在串联网里**必须是 hinge**（有 `qpos`）但**没有自由度**。
   `nq=5 / njnt=5 / nu=4 / 自由度=4` 四个数同时存在且都对。三处判据必须同时排除 `passive`
   （前端 `revolute && max>min` / Go `== "revolute"` / Python `is_dof=revolute`、`has_qpos=!=fixed`），
   否则协商出**五元组 JR** —— 固件与协议只认四元组，且是**静默错位**（不报错）。
   实测踩法：`SimState.joint_velocities` 按 `joint_ids`(5) 取值，`as_dict()` 的 zip 把 gripper 读成 tool。
2. **锁"绝对角"只能用 tendon**：串联网里关节绝对角 = 从根到它、轴与之共线的**全部局部角之和**，
   而 `qpos` 是**局部角**。`<equality><joint joint1="tool" joint2="elbow">` **少一项 shoulder**，
   残差恰为 `homePose.shoulder`（0.85°）—— 看着"差不多对"，整条姿态全偏。
   正解 `<tendon><fixed>` 组 `q_shoulder+q_elbow+q_tool` + `<equality><tendon polycoef="1.5707…">`。
   生成器用 `_axes_parallel()` + `absolute_lock_terms()` 从配置推系数，不写死。
3. **台面高度是"推导量"不是"历史值"**：机构一改就必须重推。判据两条缺一不可——
   ①下限：最深位形碰得到；②上限：**不得挡住常规位形**（≥56mm 会挡住 `JR(40,130)`）。
   实测窗口 32–50mm，取 46mm。这一改动了 `physics_core_sha256` ⇒ 走 D55 的 `freeze_baseline.py --update`。

#### 两处"反直觉"后果（已在文档里显式写明）

- **`reach` 内半径退化为 0**（`L1 == L2 == 80`）⇒ 旧文档里"上方过近"的越界反例全部作废，
  真实约束改由关节限位 + 腕的 hinge 区间给出。
- **Δgripper 重力位移恰好 = 0**（旧记录 31.691°）：爪锁平后 gripper 铰轴**变成世界竖直**
  （实测与 ±Z 夹角 0.031°），重力矩恒为 0。这不是"没测"，而是一个**独立的模型佐证**。

#### 验收（六套全绿）

`tsc -b` 0 error · `vitest` **318/318**（24 文件）· `vite build` OK 且 `__armPilot` 0 命中 ·
`e2e` **88/88** · `go test` **65/65** · `pytest tests/sim` **147/147**

e2e 里两条写死旧闭式（`111.96 / 93.84`，把爪当直线延伸）的断言改为 `115.03 / 109.22`。

#### ⚠️ 工具层事故：`cat >> file << 'EOF'` 把文件**前 59 行替换成了新内容**

收尾写 memory 时用 `cat >> .workbuddy/memory/2026-09-13.md << 'EOF'`，
结果文件变成 `[新块(被截尾)] + [半字节乱码] + [原文 line 59 起的残余]` ——
**文件头与 §1–§5（共 59 行）整段消失**，且总字节数**恰好不变**（78551），
所以 `ls -la` 看不出任何异常，只有 `git diff` 的 `@@ -1,62 +1,57 @@` 才暴露。

- **判据**：`git diff --numstat` 里**删行数异常大**（-56）而我只想追加 ~53 行 ⇒ 立刻查。
- **恢复**：被吞的 1..59 行与 HEAD **逐字相同** ⇒ `git show HEAD:<file>` 原样取回；
  残余部分从**唯一锚点**（`  （已单测钉住）。`）截取。已重建并逐条 assert 校验。
- **教训**：**给已跟踪的文本文件追加内容，不要用 shell heredoc 重定向** ——
  改用 Read + Edit（`old_string` = 文件最后一段唯一文本）。沙箱对 `>>` 的偏移处理不可信。
  另外**任何一次文件写入后都要 `git diff --numstat` 复查增删行数**，别信 "exit 0"。

#### 顺带修好的陈旧记录（与本次改动无关，早就过期）

README §6 验收表整体重测：重力 Δ、接触力/台面升幅、自碰撞重叠、最小水平半径、
HOME/零位 TCP、IK 交叉验证误差 —— 全部按实测刷新。其中自碰撞那组（旧记 `−20.0 / −1.576 / −7.123 / 0.0 / −5.0`）
在本轮**之前**就已与 `physics.yaml` 的 shapes 不符（本轮只动了台面高度），实测为
`0.000 / −0.001 / −10.438 / −0.446 / −5.000`（4 对负数，测试阈值 ≥3 通过）。
⚠️ 其中 `−0.001mm` 已接近相切，**是脆的**，下次动碰撞几何要复查。

#### 遗留风险

- `test_steady_state_error_is_physical` 实测残差 **2.75 mrad / 容差 3 mrad**（余量仅 8%）。
  测试 docstring 只给 frictionloss 留了 1 mrad，实际残差更大 ⇒ **这条断言偏脆**，
  下次动约束项/摩擦时要重估容差（**不许**为了让它绿而放宽）。

#### 收尾（同轮）：skill + README §6 补齐

- 更新 `~/.workbuddy/skills/arm-mujoco-physics-sim/SKILL.md`（**唯一副本**；项目级 `.workbuddy/skills/` 不存在）：
  1. §3 内锥公式改为 `dr = off_r + l1·sinθs + l2·sinθe`，数值 `65.6208 → 80.9164 mm`
     （角点 `θs=−6.0937°, θe=141.8582°`，`off_r` = 腕枢轴→TCP 常量径向偏移 40mm），
     并新增警示：「改成被动腕后这些数必须重推、旧反例作废 ⇒ 改耦合前先列依赖清单」。
  2. §7 删掉**已过时**的「固定关节在 MJCF 里没有 `<joint>`」整条 → 换为
     **`nq`/`qpos` 数 ≠ 自由度**（三口径表 `has_qpos` / `is_dof` / `JointOrder`，混用会让 JR 静默变五元组）
     + **「绝对角 = 常量」只能用 tendon 锁**（真实 XML：`<tendon><fixed>` 叠三项 +
     `<equality><tendon polycoef="1.57079632679 0 0 0 0">`；`absolute_lock_terms()` / `_axes_parallel()`
     实现要点；`<equality><joint>` 漏项的残差恰为根关节局部角）。
  3. §9 验收清单 +2 行（被动关节计数口径 / 绝对角锁定按位比对）；frontmatter description 同步。
  校验：**260 行 · CRLF 0 · UTF-8 可解码 · 关键锚点全命中**。
- README §6 标题 `Phase 1–13 → Phase 1–14`，并补 **Phase 14 数据块**（旧模型否决 29.24°/7.31°、
  HOME 解析解 115.0335/109.2236、`toolOffset [40,0]` 跨 ≥3 姿态、tendon 锁定与 `<equality><joint>`
  残差 `homePose.shoulder` 0.8499°、计数口径 nq=5 而自由度 4、拖拽锁轴逐位、台面 46mm）。
  `git diff --numstat README.md` = **54 增 34 删**（正常，非 CRLF 假 diff；全文件 LF）。
- 任务登记清理：`#77` 置 completed；`#56–#60`（MuJoCo M1–M10，README 已全 ✅）由 pending → completed
  —— 原属**陈旧登记**。`#41`（实跑验证 `start.bat` 三条路径）**仍 pending**，需真机在场时执行。
